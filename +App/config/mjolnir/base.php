<?php return array
	(
		'logging' => array
			(
				// the short.sql.log logs a short version of database queries,
				// a SELECT will get logged as fields, FROM, LIMIT, OFFSET, and
				// WHERE (ie. ommiting JOINs, etc)
				'short.sql.log' => false,
			),
	
	); # config
